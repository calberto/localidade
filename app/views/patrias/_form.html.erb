<%= form_for(patria) do |f| %>
  <% if patria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patria.errors.count, "error") %> prohibited this patria from being saved:</h2>

      <ul>
      <% patria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="field">
    <%= f.label :sigla %>
    <%= f.text_field :sigla %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
